#include <bits/stdc++.h>

using namespace std;

int main() {
    int _; cin >> _;
    while(_--) {
        int maxel = INT_MIN, minel = INT_MAX;
        int n; cin >> n;
        vector<int> a(n);
        for(auto& x: a) {
            cin >> x;
            maxel = max(maxel, x);
            minel = min(minel, x);
        }
        for(int i = 1; i < n; i++) {
            if()
        }
    }
}